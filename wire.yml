env:
    class: Component::Env

log:
    class: Component::Log
    args:
        env:
            $ref: env
        filename: 'logs/application.log'

numid_gen:
    class: Component::NumericIdGenerator

